# ТЗ: Format Management
## Мини-CRM для управления заявками в компании

**Версия:** 2.0  
**Дата:** 19 февраля 2026  
**Статус:** Готово к разработке

---

## 1. Описание проекта

**Format Management** — веб-приложение для управления заявками (заказами) внутри одной организации. Система позволяет различным уровням сотрудников просматривать, создавать и управлять заявками в зависимости от их ролей.

**Основная цель:** централизованное управление заявками, отслеживание процесса выполнения, оценка эффективности сотрудников и получение основной аналитики.

---

## 2. Технологический стек

### Frontend
- **Vue.js 3** + TypeScript
- **Vite** — сборщик и dev-сервер
- **Pinia** — управление состоянием (store)
- **Vue Router** — маршрутизация
- **Axios** — HTTP клиент для API
- **TanStack Table** или **AG Grid** — компонент для таблицы заявок
- **Tailwind CSS** или **Vuetify** — UI компоненты

### Backend
- **PHP 8.2+**
- **Laravel 11+** — веб-фреймворк
- **Eloquent ORM** — объектно-реляционное отображение
- **Laravel Sanctum** — API аутентификация (token-based)
- **Gates & Policies** — система авторизации и RBAC
- Composer — менеджер зависимостей

### Database
- **MySQL 8.0+** на выделенном сервере **Beget** (без VPS)

### Deployment
- **Хостинг:** Beget выделенный сервер (Apache/Nginx + PHP + MySQL)
- **Frontend:** статические файлы (dist/) или через Laravel
- **Backend:** Laravel API на том же сервере

---

## 3. Функциональные требования

### 3.1 Система ролей и доступа

**Три роли с разными уровнями доступа:**

#### 1. **Работник (WORKER)**
- ✅ Может просматривать только свои заявки
- ✅ Может создавать новые заявки
- ✅ Может редактировать свои заявки (если статус = "new" или "in_progress")
- ❌ Не видит заявки других работников
- ❌ Не может удалять заявки

#### 2. **Менеджер (MANAGER)**
- ✅ Видит все заявки в своей команде
- ✅ Может создавать заявки
- ✅ Может редактировать статусы заявок команды
- ✅ Видит базовую аналитику (количество, статус)
- ❌ Не может удалять заявки

#### 3. **Руководитель / Администратор (ADMIN)**
- ✅ Полный доступ ко всем заявкам
- ✅ Может управлять пользователями (создание, редактирование, удаление)
- ✅ Может менять роли пользователей
- ✅ Видит расширенную аналитику
- ✅ Может удалять заявки
- ✅ Доступ к админ-панели

---

### 3.2 Основной функционал

#### A. Аутентификация и авторизация

**Регистрация:**
- Email и пароль
- Пароль минимум 8 символов, с буквами и цифрами
- Проверка на уникальность email
- Новый пользователь автоматически получает роль **WORKER**

**Вход (Login):**
- Email + пароль
- Выполняется через REST API
- При успешном входе возвращается API token (Sanctum)
- Token хранится на клиенте в `localStorage`
- Все последующие запросы содержат заголовок `Authorization: Bearer <token>`

**Выход:**
- Удаление токена на сервере
- Удаление токена на клиенте

**Восстановление пароля:**
- Форма "Забыли пароль" (email)
- Email с ссылкой сброса
- Изменение пароля через уникальный токен

#### B. Управление заявками

**Создание заявки:**
- Форма с полями:
  - **Название** (title) — обязательное, макс 255 символов
  - **Описание** (description) — опционально, макс 2000 символов
  - **Статус** (status) — default "new" (новая)
  - **Дата создания** — автоматическая (created_at)

**Просмотр заявок:**
- Таблица со всеми заявками (с учетом ролей)
- Колонки: ID, Название, Статус, Автор, Дата создания, Дата обновления
- Сортировка по любой колонке
- Фильтрация по статусу
- Поиск по названию/описанию
- Пагинация (15 заявок на странице)

**Редактирование заявки:**
- Работник может редактировать только свои заявки
- Менеджер может редактировать заявки своей команды
- Администратор может редактировать любые заявки
- Изменяемые поля: название, описание, статус
- Логирование изменений

**Удаление заявки:**
- Только администратор может удалять заявки
- Возможность мягкого удаления (soft delete или флаг archive)

**Статусы заявки:**
- `new` — новая
- `in_progress` — в процессе
- `completed` — завершена
- `cancelled` — отменена

#### C. Управление пользователями (для администратора)

**Список пользователей:**
- Таблица со всеми пользователями (для админа)
- Колонки: ID, Имя, Email, Роль, Дата регистрации
- Фильтрация по роли
- Поиск по имени/email
- Пагинация

**Редактирование пользователя:**
- Администратор может менять роль
- Администратор может редактировать имя/email

**Удаление пользователя:**
- Администратор может удалить пользователя
- При удалении его заявки остаются в системе (связь не удаляется)

---

### 3.3 Аналитика и статистика

#### Для Менеджера:
- Количество заявок в статусе "new", "in_progress", "completed"
- Средний срок выполнения заявок
- Список самых активных работников

#### Для Администратора:
- Все метрики менеджера
- График создания заявок по датам (за последний месяц)
- Статистика по ролям (сколько worker, manager, admin)
- Экспорт отчета в CSV (опционально)

---

## 4. Архитектура приложения

### 4.1 Backend структура (Laravel)

```
backend/
├── app/
│   ├── Http/
│   │   ├── Controllers/
│   │   │   ├── AuthController.php           # Регистрация, вход, выход
│   │   │   ├── OrderController.php          # CRUD заявок
│   │   │   ├── UserController.php           #管理 пользователей
│   │   │   └── AnalyticsController.php      # Аналитика и статистика
│   │   ├── Middleware/
│   │   │   ├── EnsureTokenIsValid.php       # Проверка токена (Sanctum)
│   │   │   └── CheckRole.php                # Проверка ролей
│   │   └── Requests/
│   │       ├── LoginRequest.php
│   │       ├── RegisterRequest.php
│   │       ├── StoreOrderRequest.php
│   │       └── UpdateOrderRequest.php
│   ├── Models/
│   │   ├── User.php                         # Модель пользователя
│   │   └── Order.php                        # Модель заявки
│   ├── Policies/
│   │   ├── OrderPolicy.php                  # Авторизация заявок
│   │   └── UserPolicy.php                   # Авторизация пользователей
│   └── Providers/
│       └── AppServiceProvider.php
├── database/
│   ├── migrations/
│   │   ├── create_users_table.php
│   │   └── create_orders_table.php
│   └── seeders/
│       └── UserSeeder.php
├── routes/
│   └── api.php                              # REST API маршруты
├── .env.example
└── composer.json
```

### 4.2 Frontend структура (Vue 3 + TypeScript)

```
frontend/
├── src/
│   ├── components/
│   │   ├── layout/
│   │   │   ├── AppHeader.vue                # Шапка с навигацией
│   │   │   ├── AppSidebar.vue               # Боковое меню
│   │   │   └── AppLayout.vue                # Основной layout
│   │   ├── orders/
│   │   │   ├── OrdersTable.vue              # Таблица заявок
│   │   │   ├── OrderForm.vue                # Форма создания/редактирования
│   │   │   └── OrderDetail.vue              # Детальный просмотр
│   │   ├── users/
│   │   │   └── UsersTable.vue               # Таблица пользователей
│   │   ├── analytics/
│   │   │   ├── StatsPanel.vue               # Панель статистики
│   │   │   └── Charts.vue                   # Графики
│   │   └── ui/
│   │       ├── Button.vue
│   │       ├── Input.vue
│   │       ├── Modal.vue
│   │       └── Table.vue
│   ├── views/
│   │   ├── LoginView.vue                    # Страница входа
│   │   ├── RegisterView.vue                 # Страница регистрации
│   │   ├── DashboardView.vue                # Главная страница
│   │   ├── OrdersView.vue                   # Список заявок
│   │   ├── OrderDetailView.vue              # Детали заявки
│   │   ├── UsersView.vue                    # Управление пользователями
│   │   ├── AnalyticsView.vue                # Аналитика
│   │   └── NotFoundView.vue                 # 404
│   ├── services/
│   │   ├── api.ts                           # Axios конфигурация
│   │   ├── authService.ts                   # Работа с аутентификацией
│   │   ├── orderService.ts                  # API для заявок
│   │   └── userService.ts                   # API для пользователей
│   ├── stores/
│   │   ├── authStore.ts                     # Pinia store для пользователя
│   │   └── orderStore.ts                    # Pinia store для заявок
│   ├── router/
│   │   └── index.ts                         # Vue Router конфигурация
│   ├── utils/
│   │   ├── formatDate.ts
│   │   └── validators.ts
│   ├── App.vue
│   └── main.ts
├── public/
├── index.html
├── vite.config.ts
├── tsconfig.json
└── package.json
```

---

## 5. REST API Endpoints

### 5.1 Authentication (`/api/auth`)

| Метод | Endpoint | Авторизация | Описание |
|-------|----------|-------------|---------|
| POST | `/auth/register` | ❌ Публичный | Регистрация нового пользователя |
| POST | `/auth/login` | ❌ Публичный | Вход по email и паролю |
| POST | `/auth/logout` | ✅ Sanctum | Выход и удаление токена |
| GET | `/auth/me` | ✅ Sanctum | Получить текущего пользователя |
| POST | `/auth/forgot-password` | ❌ Публичный | Запрос на восстановление пароля |
| POST | `/auth/reset-password` | ❌ Публичный | Сброс пароля по токену |

**Request/Response примеры:**

```json
POST /api/auth/register
Content-Type: application/json

{
  "name": "John Doe",
  "email": "john@example.com",
  "password": "SecurePass123"
}

Response (201):
{
  "user": {
    "id": 1,
    "name": "John Doe",
    "email": "john@example.com",
    "role": "worker",
    "created_at": "2026-02-19T10:00:00Z"
  },
  "token": "eyJ0eXAiOiJKV1QiLCJhbGc..."
}
```

### 5.2 Orders (`/api/orders`)

| Метод | Endpoint | Авторизация | Ограничения |
|-------|----------|-------------|------------|
| GET | `/orders` | ✅ Sanctum | Работник видит свои, менеджер видит команды, админ видит все |
| GET | `/orders/{id}` | ✅ Sanctum | Автор, менеджер (если в команде) или админ |
| POST | `/orders` | ✅ Sanctum | Работник, менеджер, админ |
| PATCH | `/orders/{id}` | ✅ Sanctum | Автор (если new/in_progress), менеджер (своей команды), админ |
| DELETE | `/orders/{id}` | ✅ Sanctum | Только админ |

**Request/Response примеры:**

```json
POST /api/orders
Authorization: Bearer <token>
Content-Type: application/json

{
  "title": "Исправить логин форму",
  "description": "Пользователям сложно вводить пароль двойной клик срабатывает",
  "status": "new"
}

Response (201):
{
  "id": 1,
  "title": "Исправить логин форму",
  "description": "...",
  "status": "new",
  "user_id": 1,
  "user": {
    "id": 1,
    "name": "John Doe"
  },
  "created_at": "2026-02-19T10:00:00Z",
  "updated_at": "2026-02-19T10:00:00Z"
}
```

### 5.3 Users (`/api/users`)

| Метод | Endpoint | Авторизация | Ограничения |
|-------|----------|-------------|------------|
| GET | `/users` | ✅ Sanctum | Только админ |
| GET | `/users/{id}` | ✅ Sanctum | Только админ |
| PATCH | `/users/{id}` | ✅ Sanctum | Только админ |
| DELETE | `/users/{id}` | ✅ Sanctum | Только админ |

### 5.4 Analytics (`/api/analytics`)

| Метод | Endpoint | Авторизация | Ограничения |
|-------|----------|-------------|------------|
| GET | `/analytics/dashboard` | ✅ Sanctum | Менеджер и админ |
| GET | `/analytics/orders-by-status` | ✅ Sanctum | Менеджер и админ |
| GET | `/analytics/orders-trend` | ✅ Sanctum | Админ |

---

## 6. Сценарии использования (Use Cases)

### 6.1 Работник: Создание и просмотр своих заявок

1. **Вход в систему:**
   - Открыть `login` → ввести email/пароль → попадает на Dashboard

2. **Просмотр своих заявок:**
   - На странице `/orders` видит только свои заявки
   - Может сортировать по дате, статусу
   - Может искать по названию

3. **Создание новой заявки:**
   - Кнопка "Новая заявка" → открывается модалка/форма
   - Вводит название и описание
   - Нажимает "Создать" → заявка появляется в списке статусом "new"

4. **Редактирование своей заявки:**
   - Выбирает заявку со статусом "new" или "in_progress"
   - Нажимает "Редактировать" → может изменить название/описание
   - Нажимает "Сохранить"

---

### 6.2 Менеджер: Управление командой

1. **Просмотр заявок команды:**
   - На `/orders` видит заявки всех работников своей команды
   - Видит столбец "Автор" и "Статус"

2. **Изменение статуса заявки:**
   - Выбирает заявку → редактирует статус (new → in_progress → completed)
   - История изменений логируется

3. **Просмотр аналитики:**
   - На `/analytics` видит:
     - Количество заявок по статусам
     - Самых активных работников
     - График создания заявок

---

### 6.3 Администратор: Полный контроль

1. **Управление пользователями:**
   - На `/users` видит всех пользователей
   - Может менять роли (worker → manager → admin)
   - Может удалять пользователей

2. **Просмотр всех заявок:**
   - На `/orders` видит ВСЕ заявки в системе
   - Может редактировать и удалять любую заявку

3. **Полная аналитика:**
   - На `/analytics` видит все метрики
   - Может видеть график тренда заявок за месяц
   - Может видеть распределение по ролям

---

## 7. Требования к базе данных

### 7.1 Таблница `users`

| Поле | Тип | Ограничения | Описание |
|------|-----|------------|---------|
| `id` | BIGINT | PRIMARY KEY, AUTO_INCREMENT | Уникальный ID |
| `name` | VARCHAR(255) | NOT NULL | Имя пользователя |
| `email` | VARCHAR(255) | NOT NULL, UNIQUE | Email |
| `password` | VARCHAR(255) | NOT NULL | Хешированный пароль (bcrypt) |
| `role` | ENUM('worker','manager','admin') | DEFAULT 'worker' | Роль |
| `email_verified_at` | TIMESTAMP | NULL | Дата верификации email |
| `created_at` | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | Дата создания |
| `updated_at` | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP ON UPDATE | Дата обновления |

### 7.2 Таблица `orders`

| Поле | Тип | Ограничения | Описание |
|------|-----|------------|---------|
| `id` | BIGINT | PRIMARY KEY, AUTO_INCREMENT | Уникальный ID |
| `title` | VARCHAR(255) | NOT NULL | Название заявки |
| `description` | TEXT | NULL | Описание |
| `status` | ENUM('new','in_progress','completed','cancelled') | DEFAULT 'new' | Статус |
| `user_id` | BIGINT | NOT NULL, FOREIGN KEY | ID автора заявки |
| `created_at` | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | Дата создания |
| `updated_at` | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP ON UPDATE | Дата обновления |

### 7.3 Таблица `personal_access_tokens` (Laravel Sanctum)

| Поле | Тип | Ограничения | Описание |
|------|-----|------------|---------|
| `id` | BIGINT | PRIMARY KEY, AUTO_INCREMENT | Уникальный ID |
| `tokenable_id` | BIGINT | NOT NULL | ID пользователя |
| `tokenable_type` | VARCHAR(255) | NOT NULL | Тип модели (всегда "App\\Models\\User") |
| `name` | VARCHAR(255) | NOT NULL | Имя токена |
| `token` | VARCHAR(80) | NOT NULL, UNIQUE | Уникальный токен |
| `abilities` | TEXT | NULL | JSON массив прав |
| `last_used_at` | TIMESTAMP | NULL | Когда был последний раз использован |
| `created_at` | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | Дата создания |
| `updated_at` | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP ON UPDATE | Дата обновления |

---

## 8. Non-Functional Requirements

### 8.1 Производительность
- Загрузка списка заявок: **< 1 сек** (при 10K записей)
- Поиск по заявкам: **< 500 мс**
- API response time: **< 200 мс**

### 8.2 Безопасность
- ✅ Все пароли хешируются через **bcrypt**
- ✅ **SQL Injection** защита через Eloquent ORM
- ✅ **CSRF** защита (CSRF токен в формах)
- ✅ **XSS** защита (Blade escaping, Vue sanitization)
- ✅ **HTTPS** обязателен на продакшене
- ✅ **CORS** настроен для API
- ✅ Rate limiting для API endpoints

### 8.3 Масштабируемость
- Поддержка до 500 пользователей без проблем
- Индексы на `user_id`, `status`, `created_at` в таблице `orders`
- Кеширование часто запрашиваемых данных (опционально)

### 8.4 Надежность
- Миграции БД версионированы
- Логирование всех критических операций
- Обработка ошибок и валидация на обе стороне (front + back)
- Backup стратегия для MySQL

---

## 9. Фазы разработки

### Фаза 1: Backend Setup (1-2 дня)
- [ ] Инициализировать Laravel 11+ проект
- [ ] Настроить MySQL, миграции
- [ ] Создать модели User и Order
- [ ] Реализовать аутентификацию (Sanctum)
- [ ] Реализовать контроллеры для Orders и Users
- [ ] Добавить Policies для RBAC
- [ ] Протестировать API вручную (Postman/Insomnia)

### Фаза 2: Frontend Setup (1-2 дня)
- [ ] Инициализировать Vue 3 + TypeScript + Vite проект
- [ ] Настроить Vue Router и основные маршруты
- [ ] Создать Pinia store для Auth и Orders
- [ ] Настроить Axios для API запросов
- [ ] Создать layout и главные компоненты

### Фаза 3: Frontend Development (3-4 дня)
- [ ] Страница Login / Register
- [ ] Страница Orders (таблица, фильтры, создание)
- [ ] Страница Users (админ-панель)
- [ ] Страница Analytics
- [ ] Интеграция с Backend API
- [ ] Обработка ошибок, уведомления

### Фаза 4: Testing & Polish (1-2 дня)
- [ ] End-to-end тестирование
- [ ] Баг-фиксы
- [ ] Оптимизация
- [ ] Подготовка к деплою

### Фаза 5: Deployment (1 день)
- [ ] Деплой на Beget (выделенный сервер)
- [ ] Настройка MySQL и миграции
- [ ] Настройка веб-сервера (Apache/Nginx + PHP)
- [ ] SSL сертификат
- [ ] Финальное тестирование в prod

---

## 10. Критерии приемки

✅ **Backend:**
- [ ] Все API endpoints работают согласно спецификации
- [ ] RBAC работает корректно для всех ролей
- [ ] Валидация данных на всех форм-запросах
- [ ] Обработка ошибок с правильными HTTP кодами
- [ ] Аутентификация через Sanctum работает стабильно

✅ **Frontend:**
- [ ] Все страницы отображаются корректно
- [ ] Сортировка, фильтрация, поиск работают
- [ ] Созданные/отредактированные данные синхронизируются с Backend
- [ ] RBAC соблюдается (пользователи не видят запрещенные данные)
- [ ] Нет console errors в браузере
- [ ] Мобильная версия работает (responsive design)

✅ **System:**
- [ ] Mysql миграции применяются без ошибок
- [ ] Seed-скрипт заполняет БД тестовыми данными
- [ ] Никаких SQL errors в логах
- [ ] API response time < 200 мс
- [ ] Деплой на Beget выполнен успешно

---

## 11. Дополнительные заметки

### Важные моменты:
1. **Soft delete для заявок:** рекомендуется (архивирование вместо удаления)
2. **Логирование изменений:** можно добавить таблицу `activity_logs` для отслеживания кто и когда изменил заявку
3. **Email уведомления:** отправка email при создании заявки, смене статуса (опционально)
4. **Экспорт отчетов:** CSV экспорт для админа (опционально)
5. **Пагинация:** backend должен возвращать данные пагинированными (15-20 на странице)

### Примеры интеграции:
- **Компонент таблицы:** использовать TanStack Table для Vue уже есть готовые примеры
- **Формы:** использовать Vuelidate или VeeValidate для клиентской валидации
- **Уведомления:** toast notifications через компонент типа @notification или Pinia action

---

**Готово к разработке! 🚀**

Все требования четко описаны, API контракты определены, архитектура спланирована.
